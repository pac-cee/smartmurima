# Base URL of the SmartMurima API (see docs/API_CONTRACT.md)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# When "enabled", the app runs fully standalone against the MSW mock layer.
# Set to "disabled" to talk to the real backend.
NEXT_PUBLIC_API_MOCKING=enabled
